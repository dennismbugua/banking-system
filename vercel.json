{
    "builds": [{
        "src": "banking_system/wsgi.py",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "banking_system/wsgi.py"
        }
    ]
}